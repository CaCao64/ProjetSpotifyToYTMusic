# fly.toml app configuration file

app = "projetspotifytoytmusic"
primary_region = "cdg"

[http_service]
  internal_port = 3000
  force_https = true
  auto_start_machines = true
  auto_stop_machines = false

[[mounts]]
  source = "data_vol"
  destination = "/app/data"
